@* <button class="button" type="button" data-bs-toggle="modal" data-bs-target="#addAssetModal">
    &#8853; Add Assets
</button> *@
@await Html.PartialAsync("_EditAssetForm")
@{
  var category = ViewData["Category"]?.ToString() ?? "Unknown";
}

<div class="dropdown">
  <button class="btn dropdown-toggle" type="button" id="manageAssetDropdown" data-bs-toggle="dropdown"
    aria-expanded="false">
    Manage Asset
  </button>
  <ul class="dropdown-menu" aria-labelledby="manageAssetDropdown">
    @if (category.Equals("Software", StringComparison.OrdinalIgnoreCase))
    {
      <li><a class="dropdown-item" href="#" data-bs-toggle="modal" data-bs-target="#addSoftwareModal">➕ Add Software</a>
      </li>
      <li><a class="dropdown-item" href="#" id="loadSoftwareProgressBtn">💾 Load Saved Progress</a></li>
    }
    else
    {
      <li><a class="dropdown-item" href="#" data-bs-toggle="modal" data-bs-target="#addAssetModal">➕ Add Hardware</a></li>
      <li><a class="dropdown-item" href="#" id="loadProgressBtn">💾 Load Saved Progress</a></li>
      <li><a class="dropdown-item" href="#" data-bs-toggle="modal" data-bs-target="#addCategoryModal">📁 Add New
          Category</a></li>
    }
  </ul>
</div>
</div>